import { StyleSheet } from "react-native"

export const colors = {
    black: '#000',
    white:'#FFF',
    button:'#FAFAFA',
    text: '#FFECDA',
    
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})