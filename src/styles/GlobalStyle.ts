import { StyleSheet } from "react-native"

export const colors = {
    black: '#000',
    white:'#FFF'
    
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})