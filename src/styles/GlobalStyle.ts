import { StyleSheet } from "react-native"

export const colors = {
    black: '#000',
    white:'#FFF',
    button:'#FAFAFA',
    
}

export const styleContainer = StyleSheet.create({
    container: {
        flex: 1
    }
})